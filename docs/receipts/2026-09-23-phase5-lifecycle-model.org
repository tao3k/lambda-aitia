# SPDX-FileCopyrightText: 2026 tao3k team and Contributors
# SPDX-License-Identifier: Apache-2.0 AND LGPL-2.1-or-later

#+title: Phase 5 bounded lifecycle model replay receipt
#+startup: overview

:PROPERTIES:
:ID:         lambda-aitia-phase5-lifecycle-model-2026-09-23
:STATUS:     evidence-partial
:DATE:       2026-09-23
:RFC:        lambda-aitia-rfc-0001
:END:

* Exact model cut

- Aitia base commit: =611df233a21b78a35be43fbf814880fe4577a2be=;
  the model and this receipt are the candidate change on top of that commit.
- =AssuranceLifecycle.tla= SHA-256:
  =94e31fe4e68687f2deb261e50ae543d7e6434ef59afe2f869f63fb0d4267f861=.
- Safe TLC configuration SHA-256:
  =984c59bc517d3ae0a4477e7cb578f4a5a553bd966aed577d202e513679df21e9=.
- TLC2 2.19, single worker, =JAVA_TOOL_OPTIONS=-Xmx512m=, Darwin arm64.
- Replay commands and the expected invariant for every configuration are in
  =proof/tla/README.org=. Each run used a separate =-metadir= under
  =/private/tmp=; no global Gerbil/Nix environment was changed.

* Result

| Configuration | Expected | Observed |
|---------------+----------+----------|
| Safe | exit 0 | 7,468 generated; 2,202 distinct; queue empty |
| Reachable | =NoEffectStarted= counterexample | exit 12; valid effect trace |
| Unsafe report-only effect | =NoUnsafeEffectStart= counterexample | exit 12 |
| Late admission after change | =AdmissionIsCurrent= counterexample | exit 12 |
| Grant before re-verification | =GrantRequiresCurrentDecision= counterexample | exit 12 |
| Effect after revocation | =NoUnsafeEffectStart= counterexample | exit 12 |

These are local bounded-model results, not CI results. A deliberate mutant's
exit 12 is a successful *negative contract* only when TLC names the expected
invariant. The safe run's queue was empty, so the finite state space for the
declared bounds was exhausted.

* Admission boundary

This receipt supports a candidate dynamic lifecycle contract. It does not
promote a reported result to admitted evidence, establish a trusted Host
adapter/clock, cryptographically bind a Runtime grant, or close Phase 5.
Progress/fairness, counterfactual transition authority, source-bound CI replay,
and the executable Host handoff remain open. The existing POO Flow Cedar
=governance_admitted= field is not an Aitia evidence-admission decision.
